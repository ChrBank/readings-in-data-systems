Zero-shot = Don't need to add data, it works just like ChatGPT
Few-shot = Need to add some data.


To build this model, you need features that are **transferebale**
* You cannot have one hot encoding
* When do we know this amount of data used for training the model is enough.


Figure 3
* They decompose a query into a graph.
* Each layer presents the bottm up traversal.

In the MLP have different 